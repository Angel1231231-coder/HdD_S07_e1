name: a01_HdD

on:
  workflow_dispatch:

jobs:
  Mi_inicio:
    runs-on: ubuntu-latest
    steps:
      - name: "inicio"
        run: echo "Oki"


      - name: "varios"
        run: |
          date
          echo "Autor ${{github.actor}}"
